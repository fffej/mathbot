{
  "name": "mathbot",
  "version": "1.0.0",
  "description": "A bot that does math using Wolfram's API",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeff Foster",
  "license": "ISC",
  "dependencies": {
    "botbuilder": "^0.6.4",
    "restify": "^4.0.4"
  }
}
